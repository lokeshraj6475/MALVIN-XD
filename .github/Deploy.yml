name: Node.js CI

Offic

push:

branches:

main

pull request:

branches:

-main

Jobs:

build:

runs-on: ubuntu-latest

strategy:

matrix:

steps:

name: Checkout repository

uses: actions/checkout@v3

name: Set up Node.js

uses: actions/setup-node@v3

with:

node-version: Sil matrix.node-version H

-name: Install dependencies

run: npm install

-name: Start application

run: npm start
